package Task4;

/**
 * The DriverStast interface defines a method for starting a driver.
 * Classes implementing this interface must provide the start implementation.
 */
public interface DriverStast {
    // Starts the driver.
    void start();
}
